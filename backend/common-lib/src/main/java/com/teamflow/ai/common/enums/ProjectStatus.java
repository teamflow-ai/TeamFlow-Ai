package com.teamflow.ai.common.enums;

/** Lifecycle state of a project, mirroring the platform's delivery workflow. */
public enum ProjectStatus {
    PLANNING,
    ACTIVE,
    ON_HOLD,
    COMPLETED,
    CANCELLED
}
