package com.teamflow.ai.common.enums;

/** Lifecycle state of a project {@code Milestone}. */
public enum MilestoneStatus {
    PLANNED,
    IN_PROGRESS,
    READY_FOR_REVIEW,
    APPROVED,
    REJECTED
}
